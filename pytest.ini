[pytest]
pythonpath = . app
addopts = -q
markers =
    unit: deterministic tests without external services
    integration: local component integration tests
    docker: tests requiring Docker
    e2e: browser or complete application flow tests
    live: tests using real external exchanges
    slow: computationally expensive tests
